/**
 * Five-page photographic onboarding. Pages are state, not routes.
 */
import { OnboardingFlow } from "~/components/onboarding/OnboardingFlow";

export default function OnboardingWelcome() {
  return <OnboardingFlow />;
}
