# Example video project

Copy this directory or run the initializer described in [`docs/marketing-video-playbook.md`](../../../../docs/marketing-video-playbook.md). The `input/`, `assets/`, and `output/` directories are intentionally gitignored because they contain large local media files.

`project.json` demonstrates phrase-anchored cutaways. Replace every example path, phrase, and claim with reviewed material for the actual video.
